version: 2

seeds: 
  - name: zipcodes
    description: United states zipcodes
    config:
      column_types:
        country_code: varchar(2)
    columns:
      - name: zipcode
        tests:
          - unique
          - not-null
