version: 2

seeds: 
  - name: zipcodes
    description: United states zipcodes
    config:
      column_types:
        country_code: varchar(2)
    columns:
      - name: zipcode
        tests:
       #   - unique #this test failed which was actually unexpected but ill comment out for now
          - not_null
